/**
 * useAuth custom hook (T027).
 *
 * Convenience hook that wraps AuthContext for easier imports.
 * Re-exports useAuthContext for consistent API.
 */

export { useAuthContext as useAuth } from '../contexts/AuthContext';
